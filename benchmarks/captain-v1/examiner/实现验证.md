# 考核包实现验证

本记录评价考核工具是否按设计工作，不评价任何LLM是否能担任舰长。

后续补充已验证：当前共45项自动测试通过。新增公开包白名单及篡改/链接反例、私有答案可读使测评无效、工具stdout隐藏判分标签、单候选模拟入职考、任务书模拟接单/工件核验、上下文重建要求及虚拟时间不能冒充真实耐久。CLI实测公开包正常导出为绿、注入额外答案文件转红；未建立真实候选权限隔离，runtime_isolation_verified仍为false。

Build TUI入口补充：自动测试增至50项并全部通过。本机外层Seatbelt正反控已验证公开题面可读、考官密封清单及原题库答案不可读；客户端发现预检中活跃MCP、旧指令、hooks与plugins均为0。尚未启动候选推理，runtime_isolation_verified保留false，待候选启动后的实际工具清单和身份核验。本次目录按当前脱敏门禁检查PASS；未改门禁、未对外发布。

首轮启动修复：原Unix socket全拒绝同时拦住了macOS的DNS解析，候选一直重试。原规则curl返回6/HTTP000；只例外放行本地mDNSResponder后，同网络限制下端点返回HTTP401，文件正反控仍通过。启动前新增无凭据DNS/TLS预检，避免把连接失败送入候选答题循环。52项自动测试通过；仅重启失败的考试进程，新会话已收到模型流。没有修改Claude配置、固定出口、代理或系统DNS。基础设施故障不计候选失败；具体会话信息留在本轮私有考试回执。

| 检查 | 结果 | 证据与范围 |
|---|---|---|
| 单元/机制测试 | PASS，25项 | `python3 -m unittest discover -s tests -v` |
| 评分资格门 | PASS | 空模板不可通过、删检查点拒绝、证据缺失拒绝、每项否决归零、角色失格、困难门不能被平均分抵消 |
| 过程/结果计分 | PASS | 过程错结果对为零；过程对结果错半分；双向问错分别输出 |
| 部署模拟 | PASS | 正常部署可达、备份空壳拒迁移、先换包触发专属jar_order、迁移幂等 |
| 收件箱与激活 | PASS | 并发追加不丢、部分依赖保留、重放不重复投递、不能编造未收到的答复 |
| 终端竞态 | PASS | 读回时被人接管；误回车记否决；换合规工位可一次送达 |
| 监听 | PASS | 原生不支持；接错源虽created但无事件；改正源收到三态；重启游标不重复 |
| 画像/人员记忆 | PASS | 新反证改变探针结果；同名联系人按稳定身份分存，串人记忆被识别 |
| CLI冒烟 | PASS | 新建运行、读取能力、静默H03不投放、答Q-A只解T-B、空评分不可通过、拒绝覆盖已存在模板 |
| 文档链接 | PASS | 包内Markdown相对链接均存在；虚拟/bench坐标另按题面说明 |
| 本次写集脱敏 | PASS | 将本benchmark目录与当前门禁/词表复制到临时检查根，结构9类及37条词表无命中；28800同行已标非端口 |

开发中发现并订正：事件真值计数应为5；最初文本曾写7，测试曾预期6。现以实际alarm_key事件为准，代码测试与手册一致。原方向书权重合计110，作为A–G诊断归一化保留；用户新增职责采用单独九能力100权重。

未执行：任何候选LLM运行、真实消息送达、8小时墙钟、候选上下文真正重建、三个独立隐藏变体测评。默认入职卷无需真实舰员盲执行；它用确定性模拟器。候选实际动作和上下文重建仍须开考阶段取证，不能用本记录代替模型考核PASS。

模拟器是单考官串行CLI，状态文件由考官保管。它不是隔离真实生产的安全代理，也不是自动LLM评测平台。没有模型API适配或自动8小时调度时，按《怎么开考》由考官介导即可试跑。
