# Grok 4.6：S04 考场误拒校准

综合能力分由原始 **81.76/100** 校准为 **82.18/100**。只更正 S04.7 的模拟器误拒，不重新考核、不调用模型；原测档案和原成绩保留，困难版结论仍为未通过。

首版任务书已经完整提供 O/C 正例、Z=待核实及专属反例，并额外加入 `other=待核实`。公开素材明确其余状态均显示待核实，因此这条额外样例合法。模拟器用 unknown 字典严格等于仅含 Z 的字典，误将合法附加样例当作必须澄清的错误。

离线最小对照使用与实际运行哈希一致的冻结模拟器：原首本返回 `NEEDS_CLARIFICATION: tests`；只删 `tests.unknown.other`、其他字段完全不改，立即首次接单成功。该对照只运行确定性内存夹具，不访问候选或任何真实系统。

**真实轨迹没有被改写：Grok 实际仍在第二次接单后读取工件。** 原始 S04.7=0.5 保存为 raw_points，校准能力分为1；不得写成首次工具调用已经成功。

| 指标 | 原始 | 校准 |
|---|---:|---:|
| S04 | 6.5/7 | 7/7 |
| L2 | 53.5/58 | 54/58 |
| K4 任务书与派单质量 | 87.50 | 91.67 |
| 综合能力分 | 81.76 | 82.18 |

其余156个检查点评分完全不变，隔离、单评审、非等预算、虚拟时长和原行为缺口仍保留。此记录应作为原测的校准版本展示，不算新增独立样本。

同一问题也影响本次 Astra 的合法 X 样例，已采用相同校准口径。DeepSeek 首本则把协议要求的映射写成数组，且投递格式另有拒绝项，不属于该缺陷，不顺手改其旧分。

复算档案：`grok-4.6-s04-calibration-01`。其中 `score.py`、`examiner/cases.json`、`examiner/ratings-final.json` 可用 `python3 score.py score examiner/ratings-final.json` 复算；`S04-calibration/reproduce.py` 重做最小对照。原完整候选轨迹仍位于 `grok-4.6-01/examiner`，本档案只保存校准证据与原档案定位。
