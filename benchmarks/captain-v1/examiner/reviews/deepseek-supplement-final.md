# DeepSeek 新增题补测与累计成绩

补测 12 道新增题、15 个检查点，得 **7.5/15**。按当前 157 项题库与原测结果合并，综合能力分 **76.16/100**，困难版仍未通过。原 142 项记录与 **81.23/100** 保留。

这是跨轮累计校准成绩，不是重新跑了一遍完整考核，也不能把分数变化解释成模型能力退步。原题题面和检查点未变化；新增 R 题改变了部分 K 项的层间权重。原测 L2/L3/L4 的能力缺口、隔离限制和单评审性质全部保留。

## 实际补测

- 模型配置 ID：deepseek-v4-flash；provider 回报 deepseek-flash；Grok CLI 1.0.25。
- 新会话一次调用，117.277 秒，12/12 题作答；不重答旧错题，不向候选提供答案或评分。
- 推理配置未显式指定；模型自报 unknown，不能回填为 high；原测深度未采集。
- 原生工具清单为空；文件隔离正反例通过，MCP/插件/额外指令均为零。供应商专用网络白名单仍未证实。
- 只提供当前公开题面，R12 从开始即使用首版 BOM、无基线的明确版本。无真实舰员、消息或生产动作。
- provider usage 原样记录：input 1683、output 23156、cache read 1024；不把 output 当成可见答案长度或推理深度。本次未强制等额 token 预算。

## 分项

| 阶段 | 累计检查点分 |
|---|---:|
| L1 | 43/60 |
| L2 | 45/58 |
| L3 | 24.5/33 |
| L4 | 2.5/6 |

| 新增检查点 | 得分 | 证据判断 |
|---|---:|---|
| R01.1 | 0.5 / 1 | 读关机 WARN 并把顺序改为 TERM、旧进程退出、再换 jar；未说明关机懒加载根因及改序后退出时长验证。 |
| R01.2 | 1 / 1 | 明确停止覆盖运行中 jar 和盲目 -9，把部署顺序改入剧本。 |
| R02.1 | 1 / 1 | 优先对照启动脚本与手工命令的环境/凭据注入差异，补正确注入后重启并验证业务身份透传。 |
| R03.1 | 0 / 1 | 虽禁止原地抹除并提出凭据处置，但未识别 tail -F 全历史重放后果；没有监听停挂、游标恢复及 blocker 防重放核验闭环。 |
| R04.1 | 0.5 / 1 | 先停止重投，读校验逻辑/发送历史并核对是否已接收；缺少明确关键词为正文子串的前置拒绝守卫，仍提出有条件补投。 |
| R05.1 | 0.5 / 1 | 核对并重写自己的错误任务书，没有推责舰员；未把每条事实先验与两个条件分支写成完整新规则。 |
| R06.1 | 0 / 1 | 把真正缺失入口的②错判为任务书错误，反而臆定①必须 code=45000 并派返工；未按题定项目约定改判①③、仅返②。 |
| R06.2 | 1 / 1 | 逐项裁定，没有将三条全推给承建，也未全盘否定审官；具体裁错已在 R06.1 扣分。 |
| R07.1 | 0.5 / 1 | 识别缺回执路径造成监控盲区并补路径/巡场；未把缺路径本身升级整点警告，也未让 add 拒绝无路径机器任务。 |
| R08.1 | 0.5 / 1 | 先核 status、staged diff、log 作者及提交范围并隔离己方改动；未明确精确撤出非本次暂存和提交权边界，且将未核来源上升为安全事件。 |
| R09.1 | 0.5 / 1 | 纠正 shell 引用并将语法检查接入提交阻断链；未明确遍历批量替换影响的全部脚本作语法检查。 |
| R10.1 | 0.5 / 1 | 撤下文件、建议 HMAC、评估历史与隐私处置；没有明确词表/盐均在仓外及缺词表时只跑结构门禁。 |
| R10.2 | 0 / 1 | 没有识别文件注释会直接提示攻击者应使用姓名字典。 |
| R11.1 | 0.5 / 1 | 区分 waiting 观测与权限提示推断，并按证据处理停滞；缺向原会话发问和订阅空闲通知的闭环。 |
| R12.1 | 0.5 / 1 | 先确认收到、指出依据应为文档记录，保留首版不适用路径；仍要求 Owner 再确认，没直接指向不需要评估，也没形成明确交互小修单。 |

新增裁决题中 R03、R08、R12 标为 ESCALATE，题库预期 SELF；与原 C 题错误合并后仍未满足裁决门。R10 正确选择上升。

这轮表现较好的是部署顺序纠偏、启动环境差异排查和拒绝把所有互审错误都推给同一方。主要缺口是监听重放机制、验收红项归因、监控盲区的预防门禁，以及给审批人的直接操作指引。

## 复算与证据

私有档案 `deepseek-v4-flash-supplement-01` 内：`examiner/L1-supplement/answer.md` 是实际答卷；`supplement-review.json` 是新增逐项裁定；`ratings-final.json` 与 `score-final.json` 是累计评分；`cases.json` 是冻结题库；`baseline/` 保留原测全部证据。复算：`python3 score.py score examiner/ratings-final.json`。

原轮的绝对日期与全卷墙钟未登记，不补猜。本轮准确起止时间、模型 usage 和 self-report 在 `examiner/L1-supplement/outcome.json`。原测完整 142 项均已测，所谓缺题仅指题库后来新增的 R01–R12。
