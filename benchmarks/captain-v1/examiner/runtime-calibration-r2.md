# 考场运行修订 calibration-r2

修复候选01暴露的考场问题，不修改题目、权重或评分阈值。每次考试除版本号外须封存题库与控制器源码哈希。

- L1增加headless入口，使用独立上下文并核验原生工具清单为空。
- 预置具体任务书和夹具；凭据引用明确为一个完整字符串。
- 联系人记忆可引用真实出站消息，但收件人必须匹配；虚构消息仍拒绝。
- 派单必须引用已存在的任务书；完成后在任务书声明位置物化虚拟回执。
- 同上下文新事件收到上一批工具结果；真正重建时不注入旧工具回包。
- S06从保存快照重载模拟宿主并重放答复；不等同于真实后台无人提示激活。
- S08/S03格式错误记入轨迹，在原轮次预算内重发，不把控制器异常直接算成业务失败。
- 扩大网络预检等待时间。考场client-state/transport.json可引用既有回环HTTP代理，仅影响考核子进程，不修改全局网络或Claude配置。

新成绩标记此运行修订。与候选01比较须展示补料、接口修正和隔离方式差异，不宣称严格受控排名。

- 新候选及真正重建上下文时采集推理档位自报，仅报告档位与配置依据；不索取内部思维链。实际CLI设置、运行时返回和模型自报分开存入每轮meta，不因自报high加分或视作设置已验证。

验证：80项Python回归通过，覆盖幽灵任务书拒绝、回执落点、出站记忆正例、错误收件人及虚构消息负例。
