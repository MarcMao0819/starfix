# 术语表 · Glossary

牵星 StarFix 中英对照词表。翻译（中→英、英→中）一律以本表为准，一词一译，不得同义替换。
Bilingual term list for StarFix. Every translation in this repository follows this table exactly — one term, one rendering, no synonyms.

## 一、核心角色与机制 · Core roles and mechanisms

| 中文 | English | 备注 / Note |
|---|---|---|
| 牵星 | 牵星 (StarFix) | 项目名保留中文，首次出现附 StarFix / project name stays in Chinese, followed by StarFix on first use |
| 舰长 | captain | |
| 主窗 / 主窗口 | main window | 舰长所在的那个常驻会话 / the captain's resident session |
| 舰员 | crew member | 复数 crew members；集合名词用 crew |
| 指挥官 / Owner | Owner | 中文两种写法在英文里统一成 Owner |
| 对接人 | stakeholder | 业务侧的人（质量/生产/财务/商务） |
| 任务书 | task book | |
| 回执 | receipt | |
| 三态 | three terminal states (PASS/FAIL/BLOCKED) | 单独指某一态时用 terminal state |
| 任务激活器 | task activator | 简称「激活器」= activator |
| 请示浮窗 | ask panel | |
| 收件箱 | inbox | |
| 决策面板 | decision panel | |
| 离席模式 | away mode | |
| 并线 / 波 | merge / wave | 「一波并线」= one merge wave |
| 门 / 门禁 | gate | 「过门」= pass the gates |
| 判据 | criterion | 复数 criteria |
| 判别力 | discriminability | 「有判别力」= discriminating |
| 战例 | case file | 「落一条战例」= file a case |
| 踩坑 | pitfall | |
| 机检 | machine check | |
| 轨迹编译 | Trajectory Compiler | 作动词时 compile the trajectory |
| 归纳器 | inducer | |
| 运行器 | runner | |
| 轮询器 | poller | |
| 漂移 | drift | |
| 版本真相 | version truth | 版本真相文件 = version-truth file |
| 先停后换 | stop-then-swap | |
| 自伤 | self-inflicted incident | 「自伤率」= self-inflicted incident rate |
| 上升 | escalate | 「上升项」= escalation |
| 自决 | decide autonomously | 「自决项」= autonomous decision |
| 备案 | file (a note) | 「备案通过」= ruled and filed |
| 克隆槽位 | clone slot | |
| 一次性库 | throwaway database | |
| 两步投递 | two-step delivery | |
| 关键词 | keyword | 投递读回用的那个词 |
| 整点报 | hourly report | |
| 停工哨兵 | stall sentinel | |
| 断流看门狗 | disconnect watchdog | |
| 水位 | context watermark | 上下文水位；`used_percentage` |
| 交接 | handoff | |

## 二、占位符映射 · Placeholder mapping

翻译时占位符一并译成英文，形状（尖括号）保持不变。
Placeholders are translated too; the angle-bracket shape is preserved.

| 中文占位 | English placeholder |
|---|---|
| `<日期>` | `<date>` |
| `<代号>` | `<code>` |
| `<单号>` | `<task ID>` |
| `<角色>` | `<role>` |
| `<项目>` | `<project>` |
| `<外部ERP>` | `<external ERP>` |
| `<业务域A>` / `<业务域B>` | `<domain A>` / `<domain B>` |
| `<模型A>` / `<模型B>` | `<model A>` / `<model B>` |
| `<质量负责人1>` | `<QA lead 1>` |
| `<生产负责人1>` | `<production lead 1>` |
| `<采购负责人>` | `<procurement lead>` |
| `<销售对接人1>` / `<销售对接人2>` | `<sales stakeholder 1>` / `<sales stakeholder 2>` |
| `<商务客服>` | `<commercial support>` |
| `<同事A>` | `<colleague A>` |
| 舰员甲 / 乙 / 丙 / 丁 | Crew A / B / C / D |
| 舰员戊 / 己 / 庚 / 辛 / 壬 | Crew E / F / G / H / I |

## 三、常用短句定译 · Fixed renderings

| 中文 | English |
|---|---|
| 回执不是事实，实物才是 | a receipt is not a fact; the artifact is |
| 它红过吗 | has it ever gone red? |
| 什么情况下它会红 | under what condition would it go red? |
| 只报危险侧 | report only the dangerous side |
| 只报状态跃迁 | report only state transitions |
| 不轮询 | never poll |
| 检查点不停手 | do not stop at checkpoints |
| 收到即抽查 | spot-check on arrival |
| 一票否决 | automatic disqualification |
| 收尾条款 | closing clause |
| 读取白名单 | read whitelist |
| 承建 | build (the crew member who builds = the builder) |
| 复审 / 互审 | review / cross-review |
| 派单 | dispatch |
| 验收 | acceptance |
| 台账 | ledger |
| 画像 | profile |
| 剧本 | runbook |
| 业务心跳 | business heartbeat |
| 缺省落安全侧 | the default branch must fall on the safe side |
| 反例 | counterexample |
| 正例锚 | positive anchor |
| 两态自证 | two-state self-proof |
