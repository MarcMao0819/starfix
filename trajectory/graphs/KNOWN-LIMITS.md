# 各图的已知边界（域主裁定记录）

本文件记录**图在原理上查不出**的违规类型。这类边界不是 bug，是判据的能力边界；写在这里防止后人误以为"图跑绿了就一定合规"。

## 图② 回执合规

### L-2.1 漏写型打回违规（<日期> 域主裁定，二期不做，留三期）

**边界**：某单被校验官打回过，但回执里**完全没写**任何追加节、也没在状态字段留痕——这种"漏写型违规"**文件内机械判据原理上查不出**（文件里没有任何痕迹可查）。

**为什么查不出**：r6 的触发依赖文件自身留下的结构化标记（打回类节标题 / 状态字段含打回）。漏写者恰恰是不留标记的人，属于自证问题。

**要查出来需要什么**：校验官判决流当**外部输入**做交叉核对——即"谁被打回过"这个事实必须来自回执文件之外的数据源（校验官判决记录），再与回执内容比对。

**处置**：二期不做。三期接判决流后补。在此之前，图②对该类违规的漏检是**已知且被接受的**。

### L-2.2 生效日不做逻辑（<日期> 域主裁定）

收尾条款 <日期> 生效，此前的历史单（如 CS-<日期>-M2 缺状态字段）按现判据会判 FAIL。裁定：**判 FAIL 属实但不回改历史，图不做生效日分支**；这类单在输出清单里由人工标注「条款前历史单」。

图保持无状态、无日期逻辑——这是有意的简化，不是遗漏。

---

# 待修缺陷（**与上面不同：这些是 bug，不是能力边界**）

上面记的是「原理上查不出」，下面记的是「本该查得出却查错了」。两类不许混——把 bug 写成边界，等于给它发了免死金牌。

## D-1 r6 切段遇更深层子标题即收尾（<日期> 抖店线实证，已复现）

**样本**：`${FLEET_HOME}/<基座项目>ERP/抖店对接/回执/DD-02-抖店客户端.回执.md`

**实测**（我方独立复现，非转述）：

```
$ python3 runner/checks/receipt_checks_dd.py r6 <该文件>
trigger=True/reason=heading@line361/has_sha=False/has_commit_ref=False/verdict=AMBIGUOUS
```

打回节标题在 L361 `## 返修记录（DD-02-R1）`，锚点 sha `92164b67…` 就在节内 **L371**。但切段逻辑是

```python
for j in range(heading_line + 1, len(lines)):
    if re.match(r'^#{1,6}\s', lines[j]):   # ← 任何层级都算节尾
        section_end = j; break
```

`^#{1,6}` 对**任何层级**的标题都匹配，于是 L365 的 `### 返修后的工位与锚点`（是本节的**子小节**）被当成了节尾，切出来的节只有 L361-364，够不着 L371 的 sha。

**修法方向**：节尾判据应为「层级 **小于等于** 本节标题层级的下一个标题」，子标题不算节尾。

> **报单方说 r6 判 FAIL，实际机械判据出的是 AMBIGUOUS。** FAIL 是模型给的——`n9(r6)` 的 `cases` 只映射 `N/A`，AMBIGUOUS 落 `no_match` 兜给模型。**这与 dd.v1 里已修掉的 r3/r5 是同一个病**（判点缺机械分支，判决被模型随机性接管）。修 D-1 时应一并给 r6 补 `PASS/FAIL/AMBIGUOUS` 三支。

## D-2 判据不识别代码围栏，围栏内的行被当 markdown 结构（同批实证）

同一份回执里，**10 行落在 ``` 代码围栏内却以 `#` 开头**，会被 r6 的切段逻辑当成标题：

```
L72  ## git_status_summary      L90  ## likely root-cause lines
L109 ## warnings                L279 ## git_status_summary
L449 ## warnings                L511 ## HEAD
L513 ## BRANCH                  L515 ## STATUS
```

这些是回执引用的**命令原始输出**，不是文档结构。回执越是老老实实贴原始输出，越容易踩。

**波及面不止 r6**：`check_t0` 的 `has_verdict_section` 用 `^#{1,6}\s*(结论|状态)\b` 找判决小节，同样不识别围栏，原理上也会被围栏内容误导（本批未触发，但机制相同）。

**修法方向**：所有按行扫 markdown 结构的判据统一先做围栏消隐（进入 ``` 到离开 ``` 之间的行不参与结构识别）。

**当前处置**：抖店线本批由总指挥人工裁定放行。两条均记欠账，未修——修的时候要按硬约定 6 先在这份真实样本上跑出反例，再验干净样本不误报。
