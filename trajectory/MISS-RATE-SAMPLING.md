# 漏报抽样方法论（定稿 v1，<日期>）

**主窗口 <日期> 裁定 B**：挂起，等周额度重置后一次 30-50 单正经做；这两天先把方法论定稿。
**授权口径同日给定**：审查方**不读机器判词**，独立出结论后再对账。贵就贵，测漏报只有这条路。

---

## 0. 这份文档在测什么

测「**机器判 PASS 的单里，有多少其实有问题**」。

这是整条线唯一没有测量手段的方向。至今我给出的所有数字（判了多少单、拦下几单、多少走模型）都只描述了管道**做了什么**，没有一个数字描述它**漏了什么**。

## 1. 为什么误报会自己浮出来，漏报不会

| | 发生什么 | 谁会发现 |
|---|---|---|
| 误报 | 拦了不该拦的 | **有人被挡住，会来吵** → 自动暴露 |
| 漏报 | 放了不该放的 | **没有人被挡住** → 永远不会有人来说 |

所以误报的判例能自然积累（`release-gate/CASES.md` 已收若干），而**漏报判例至今零条——不是因为没有，是因为没人找过**。

## 2. 抽样框

**定义**：轮询器自动执行、终判 PASS、全程无模型裁决、无人工介入的单。

**当前实际规模：109 单**（08-06 4 单 / 08-07 49 单 / 08-08 56 单）。

排除项与理由：

| 排除 | 数量 | 理由 |
|---|---|---|
| 走过模型裁决的 | 5 单 | 已经有第二双眼睛看过，不属于"机器独自放行" |
| 出现过 FAIL 判的 | 4 单 | 已被拦下，不属于"放行" |
| 手工 run_tag 的验证轮次 | — | 是我自己的验证跑，不是生产判决 |

## 3. 分层（主窗口定）

- **风险加权层**：纯后端含迁移 / 多文件（≥5）/ 涉金额涉库存（按文件路径关键词判定）
- **纯随机层**：从剩余框内等概率抽取，保无偏
- **配比**：风险 20 + 随机 20 = 40；额度允许则 25 + 25 = 50

**两层的数字不许合并成一个「漏报率」。** 风险层的目的不是估计总体，而是**在最可能出事的地方找到个例**；总体率只能由随机层估。把两层加总平均是分层抽样最常见的错误，会得出一个既不是总体率、也不是风险率的假数字。

## 4. 盲化，以及为什么盲化还不够

### 4.1 盲化

审查方不读机器判词、不读机器中间输出。

### 4.2 必须配阳性对照

抽样框**全是 PASS 单**。审查方一旦知道（或猜到）这批都是机器放行过的，就会带着"应该没问题"的先验去看，最后报一句"全都没问题"。

这时候我们**无法区分两件事**：

> 真的没有漏报　　vs.　　审查方压根找不出问题

这正是这个项目一路在纠的老毛病——**只验期望方向，不验反向**（见 `TRAJECTORY-CONTRACT.md` 总纲）。若不做对照，本次抽样验的是"审查方没找到东西"，而不是"管道没漏东西"。

**解法**：往盲样里掺入若干**已知有问题**的单，形态与真单不可区分。审查方若连这些都放过，**它对其余单的"没问题"结论一律作废，换方法重做**。

### 4.3 阳性对照必须自造——生产里一个都没有

核查结果（<日期>，逐张查明）：生产至今共 4 张单出现过 FAIL——

| 单号 | 实况 | 是否真拦截 |
|---|---|---|
| CS-<日期>-0055 | 已裁定为误报（旧的绝对状态判据） | 否 |
| CS-<日期>-0017 | 08-07 02:25 判的 FAIL，而新判据 11:48 才上线；**用当前判据对该提交重跑，结果是 AMBIGUOUS（转人判）**，不是机械 FAIL | 否 |
| CS-<日期>-0007 | 运行器取不到提交的报错兜底 | 否（不是判决） |
| CS-<日期>-0017 | 同上 | 否（不是判决） |

> **结论：当前判据在生产里从未产生过一次确认的真拦截。**
> 推论一：阳性对照**必须自造**——用既有 fixture 机制，在真实单的副本上注入已知违规（行尾翻转 / 登记清单与实际不符 / 实体新增字段无出口），形态与真单一致。
> 推论二（须单独报给 Owner）：那条线上「检查在工作」的证据 **100% 来自 fixture，0% 来自生产**。这不等于检查无用——从不响的警报器也可能只是没着过火——但它意味着**生产数据无法佐证判据有效性**，只有这次抽样能。

## 5. 独立审查协议

| | 内容 |
|---|---|
| 审查方**拿到** | 单号、提交锚点、改动文件清单、仓库只读访问 |
| 审查方**不拿到** | 机器判词、机器中间输出、分层信息（不知道自己看的是风险层还是随机层，也不知道有无阳性对照） |
| 审查方**输出** | 自己的判词（PASS / FAIL / 说不准）+ 逐条证据 + 实际跑过的命令 |

**硬性要求**：结论必须挂证据行。「看起来没问题」不算结论，按未完成退回。

## 6. 对账判定表（开跑前写死，跑完不许改）

| 机器 | 独立审查 | 判定 | 后续动作 |
|---|---|---|---|
| PASS | PASS | 一致 | 无 |
| PASS | 说不准 | 灰区 | 记录，**不计入漏报** |
| PASS | FAIL | **候选漏报** | **必须第三方终裁**（Owner 或主窗口），终裁认了才计入 |
| PASS | 漏掉阳性对照 | **审查方失效** | 该审查方全部结论作废，换方法重做 |

**关键：审查方说 FAIL ≠ 漏报。审查方自己也会错。** 不经终裁就把它记成漏报，测出来的是**审查方的错误率**，不是管道的漏报率。

## 7. 结果怎么读（这一节必须在开跑前写死）

最可能的结果是「**一个漏报都没找到**」。届时**不许**写成"漏报率 0%"。

抽 40 单查出 0 个问题，能说的是：

> 有约 95% 的把握，真实漏报率**低于 7.5% 左右**

（统计上的三倍法则：零命中时的上界约等于 3 除以样本量。抽 50 单则约为 6%。）

也就是说：**这次抽样能排除"漏报很严重"，排除不了"漏报存在"。** 想把上界压到 1% 以下需要约 300 单，现阶段不做，把这个限制原样写进结论即可。

反过来，只要**找到一例经终裁确认的漏报**，结论立刻变硬：它证明这类问题真实存在，并给出一条可以直接补进判据的具体形态。

> 所以本次抽样**找到一例的价值远大于零命中**。方案设计应偏向"更可能找到"，而不是"数字更好看"。

## 8. 预注册纪律

开跑前必须先提交进仓库，之后不许改：

1. **抽中的单号清单**（含随机种子与抽样脚本，保证可复现）
2. **阳性对照的单号与注入的违规形态**（单独存放，审查方不可见）
3. **本文件**（判定表与解读口径）

理由：跑完再定口径，等于允许挑对自己有利的解释。这条与判据本身的硬约定 8（判据一改、此前样本作废）同源——**都是防"事后调口径"**。

## 9. 口径隔离

本方法测的是**漏报率**。

另有一种做法是让审查方看着机器判词去复核——那测的是**一致性**，是另一件事。两者的数字**不许混用、不许放进同一张表**（主窗口 <日期> 明确）。

## 10. 已知局限（写在前面，不等出结果再补）

- **审查方与被测管道可能共享同一类盲区**——同为模型，"想不到"的地方可能一样。缓解：给审查方一份与机器不同的检查清单入口。但**无法根除**，须在结论里注明。
- **抽样框只覆盖 08-06 起有留痕的单**，更早的判决无从抽样。
- **测不出"根本没进管道的单"**——本方法测的是"进来的单判得对不对"，登记环节的遗漏（该记未记、记了没触发）不在范围内，是另一个缺口。

## 11. 开跑前的清单

- [ ] 抽样脚本 + 随机种子写好并提交
- [ ] 阳性对照单造好（自造，非生产取样），形态与真单不可区分
- [ ] 抽中清单冻结提交
- [ ] 向主窗口要时间窗（它按当天舰队负载排非高峰段）
- [ ] 审查方任务书带读取白名单与唯一验证命令，禁放养探索
