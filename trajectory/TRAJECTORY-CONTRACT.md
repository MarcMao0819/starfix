# 轨迹采集约定（v1，<日期> 立）

## 总纲：所有坑的公因式 —— 只验期望方向，不验反向

两天里所有事故（误报、漏报、假绿、验证盲区）套进同一个模子：**只断言了"该发生的发生了"，没断言"不该发生的没发生"，或反过来。**

| # | 只验了（期望方向） | 漏验了（反向） | 后果 |
|---|---|---|---|
| 1 | 文件里有没有 CRLF | 这是本次引入的，还是本来就在 | 存量 CRLF 文件的正常改动被误报，两笔"并线阻断级"告警 |
| 2 | grep 找到了什么 | 找不到是不是工具差异造成的 | 假阴性，把合规文档报成缺东西，差点让人返工 |
| 3 | diff 有没有内容 | base 是不是等于 head | 空 diff 静默通过，**迁移文件漏报** |
| 4 | 名字像不像临时资源 | 它是不是正在被使用 | 在用的库和账号被当垃圾报 |
| 5 | 探活成功了 | 答复的是新进程还是老进程 | 老 JVM 假绿，新 JVM 撞端口即死，构成 5098 雷 |
| 6 | fixture 造了"本次引入问题" | 没造"存量带病、本次无辜" | 判据的误伤面从未被验证 |
| 7 | 弹窗消失了（静默化修复） | 该保留的横幅还在不在 | "整个机制全关掉"会被当成"修好了" |
| 8 | 判据整体能抓到目标 | 它靠哪个分支抓到的、别的分支是不是死的 | 冲突标记判据三个分支只有一个在工作，另两个从上线起空转；唯一在工作的那个还与正常文档撞车 |

**所以每写一条判据、每造一批 fixture、每设一个断言，都要问第二句话：**

> 反过来会怎样？**不该报的会不会被报？该报的会不会静默地不报？**

具体到动作：
- 判据写完，问「这是本次造成的还是本来就在」
- 断言写完，配一条反向断言（该消失的消失了 **且** 该保留的还在）
- fixture 造完，问「有没有造存量带病、本次无辜的样本」
- 检查跑绿了，问「它是真查过了，还是因为输入为空所以什么都没查」
- 判据抓到了，问「**靠哪一条抓到的**，其余分支是不是从来没命中过」

下面十条约定，都是这条总纲在具体场景里的落地。


三条流程各由不同采集位独立实现，格式各写各的，代价直接落在归纳器上：**为了吃下第三条流程，归纳器里被迫加了流程专属分支和一张人工映射表**。这份约定就是为了消除这类分支——采集统一了，归纳器才能真通用。

## 硬约定

1. **判决步骤必须输出 `verdict=` 字段**
   凡是决定某判决点 PASS/FAIL/N/A 的步骤，其 `output_digest` 必须含 `verdict=<PASS|FAIL|N/A|AMBIGUOUS>`，并含该判决点键（如 `point=g1` 或在 cmd 参数里带 `g1`）。
   - 反面教材：`premerge-gate` 的 digest 只有 `tracked_dirty=0 untracked=3 conflict=0`，判决点与步骤的关联无法从数据推出，只能人工映射。
   - 正面样板：`receipt-compliance` 的 `sha40_count=1/verdict=PASS`、`changeset-audit` 的 `REGEX_OK`。

2. **判据实现必须在受版本控制的仓库内**
   判据脚本一律放 `runner/checks/`，禁止留在会话临时目录（scratchpad）。
   - 事故：图② v1 的全部节点都指向 scratchpad 里的 `check.py`，临时目录一清图即报废、换机器无法复现。v2 已把脚本入库。

3. **禁用裸 `grep`**
   本机交互 shell 的 `grep` 是 ugrep 函数包装，与 `/bin/bash -c` 子进程里的系统 grep 行为不一致。存在性检查一律用 `/usr/bin/grep` 绝对路径或 Python 实现；正则禁用 `\w`/`\d`/`\s`。
   - 事故：同日两起误报，其一差点让承建方返工修一个本就合规的回执。
   - 例外：`git grep` 是 git 子命令，不受影响。

4. **同构优先于优雅**
   每轮必须跑完全相同的步骤序列，不适用的检查项也要跑并记 `N/A`。禁止为省事合并或跳过步骤——同构是归纳的前提。

5. **必须带反例**
   判决点若在全部样本里从未取到 FAIL，其 FAIL 分支就是**未经验证的人工规则**，必须在图里标 `needs_review` + `validation_required`，并在接线前用扰动注入证明可触发。
   - 现状：`premerge-gate` 的 g1/g2/g3 零反例（g4 有 <项目>-theme 的 13 个 CRLF 文件当真反例）。

7. **判据里的阈值必须参数化**
   凡判据含阈值（滞留天数、运行时长、积压数量、行数上限），阈值一律提成输入变量，**不许硬编码**。否则带时间累积门槛的判决点在一次会话内造不出反例，两态永远凑不齐。
   - 事故（<日期>）：收尾体检的 c1（滞留 > 2 天）与 c4（运行 > 8 小时）硬编码阈值，自建演练资源天生"刚建"必然 PASS，而环境里原有的陈旧资源恰好被清理掉，真 FAIL 无从取得。字面上"两态齐全"实为 **PASS 来自新判据、FAIL 来自旧判据**——直接归纳会把已被否定的旧规则学进图。
   - 正解：采样时用 `stale_days=0` / `runtime_hours=0` 取 FAIL 态，用生产值取 PASS 态，判据结构不变、只换参数。同批 c5 正是靠 `backlog_threshold` 参数化才拿到通过态的。
   - 附带好处：阈值成为运行时配置，同一张图能自检（小范围严阈值）也能巡检（全局宽阈值）。

8. **不同判据版本的样本不许混用**
   判据一改，此前采的轨迹即作废，移入 `_v<N>_旧判据存档/` 并在报告里注明。宁可重采，也不许把两套判据下的判决混着归纳——机器分不出"这条 FAIL 是按旧规矩判的"。

9. **fixture 必须覆盖"存量带病、本次改动无辜"的场景**
   只造"本次引入问题"的病态样本不够——还要造"问题早就在那、这次改动没碰它"的样本，验证判据**不会把历史存量算到当前改动头上**。
   - 事故（<日期>）：行尾判据只验了"新增一个整篇 CRLF 文件"（本次引入），没验"存量就是 CRLF 的文件正常改几行"（历史存量）。结果两笔"并线阻断级"告警全是误报——13 个文件改前改后 CR 完全相等、增删行数相等，存量行尾一根没动。误报若被采信去执行清洗（整体转 LF），才是真正的破坏。
   - 推论：**凡是检查"状态"的判据（有没有 CRLF、有没有脏、有没有残留），都要问一句：这是本次造成的，还是本来就在？** 判绝对状态会误伤，判增量才对。

10. **改共用运行器时，旧图必须保持可跑**
    运行器是多张图共用的，但**节点实现与图的 `cmd_template` 是耦合的**——就地改实现，等于让所有还在用旧图的生产调用当场失配。
    - 事故（<日期>）：升级行尾判据时把 `do_n13` 改成解析新脚本的输出格式，而生产轮询器仍在用 v4 图（旧的 for 循环命令）。解析出畸形结果，聚合节点 `do_n14` 直接 `TypeError` 崩溃，**投产的自动核验断了**。报修方一度以为是数据脏，实际是我方并发施工。
    - 规矩：① 节点实现按图自身特征分派（读 `graph["version"]` 或判断 `cmd_template` 形状），**旧图必须原样跑通且判决不变**；② 或在副本上改、验完原子切换；③ 动共用运行器前**先通知在用方**，必要时请其暂停自动调用。
    - 附带：所有从上游节点取来的集合，**元素级形状都要校验**（不只校验容器类型）；非预期形状记 `RUNNER_ERR` 并落原始值样本，绝不裸抛异常——崩溃会让调用方误判成数据问题，浪费两轮排查。
    - **施工方式（<日期> 与在用方共同定案）**：共用 runner 一律**在副本上改，切版时原子替换**（`cp` 新文件 → `mv` 覆盖），**禁止活体编辑正在被调用的脚本**。同日双方各踩一次（我改 runner 打断了对方轮询器，对方上午改轮询器也踩过）——这不是谁粗心，是共享环境施工的通病，只能靠规矩堵。
    - **双向知会**：动共用 runner 前先请在用方暂停；在用方暂停/恢复自动调用前后也知会施工方。窗口期内产生的判决按作废处理、修复后重跑，窗口期之前的判决不受影响。

11. **含"或"分支的判据，必须逐分支单独取证**
    一条判据里若有多个并列条件（正则的 `|`、若干标记文件、多个关键字），**只要有一个分支能兜住结果，其余分支是死是活从整体判决上完全看不出来**。整体判绿/判红都不能证明每个分支都在工作。
    - 事故（<日期>，fixture 实证）：冲突标记判据 `^(<<<<<<<|>>>>>>>|=======)$` 要求整行恰好等于那七个字符，而 git 写出的开头/结尾标记必带标签（`<<<<<<< HEAD`），**带后缀就匹配不上**。逐分支实测：前两个分支命中 0 个文件、只有中间那行光秃秃的等号命中——**三分之二的检出力从上线起就是死的**，而它对真冲突照样判 FAIL，所以从结果上完全看不出异常。更糟的是唯一在工作的那个分支与 markdown 一级标题下划线完全重合，正常文档会被判成"冲突未解决"。
    - 规矩：判据含并列分支时，① fixture 要能让**每个分支单独命中**，或至少逐分支单跑一次记录命中数；② 命中数为 0 的分支要么是样本没造到，要么它根本不成立——**必须查清是哪一种，不许放着**；③ 排查顺序是先问"靠哪条抓到的"，再问"这条会不会抓错"。
    - 与硬约定 5 的关系：第 5 条要的是"判决点有反例"，本条要的是"**判决点内部每个分支有反例**"。前者过了不等于后者过了——上面这条判据在判决点层面早就有反例（真冲突能抓到），照样藏着两个空转分支。

12. **判决必须绑输入指纹；输入一变，判决即失效**
    一次判决只对**它当时读到的那份输入**成立。若被审对象事后被改动而判决原样留着，那条判决就成了**过期的通行证**——它没判错，它只是不再指向现在这个东西。
    - 事故（<日期>，漏报抽样普查发现）：轮询器只取 `id > 游标` 的新单，**从不回看已判的单**。实测**全部已判单里 17 单**的登记在审完之后被改过（锚点变 / 明细变 / 两者同变），没有任何东西触发重审。典型形态：某单轮询判 DEFER（登记 4 文件、git 只有 2），随后有人把 2 行明细软删、`file_count` 改成 2，复跑即 PASS——**改动本身多半正当（返工重登），缺口在于改完没有重审**。
    - 规矩：① 判决落盘时同时记录输入指纹（此处 = `commit_hash` + 明细 `<path,action>` 排序后的哈希）；② 每轮扫描比对指纹，不等即判**旧判决失效**并重新入队；③ **失效必须留痕**（旧值→新值、变化类型、检出时间），绝不静默覆盖台账。
    - **回填不许用现状种子化**：给历史判决补指纹时，必须用「判决当时读到的值」（从留痕里取），不能用现库当前值——后者会把已经发生的漂移一笔抹平，等于假设问题不存在。
    - **重排队要问对问题**：已并线的单，「能不能放行并线」确已无意义，跑图会被入口分诊整单 SKIP，重排队就成了走过场。漂移要问的是另一件事——**「这条登记现在还自洽吗」**（锚点解析得开吗、明细与锚点对得上吗），这个问题与并没并线无关，必须单独实现、单独取证。

## 技术债：归纳器里的流程专属分支

### 已还清（<日期>）
`premerge-gate` 的人工判决点映射表与 `receipt-compliance` 的两处分支**已删除**。做法正是执行约定 1：把该流程按"判决步骤 digest 带 `point=`/`verdict=`"重采 15 轮（5 病态 fixture + 10 真实 worktree），归纳器新增通用规则 `classify_point_role_by_digest_convention()` 自动定位判决点，人工映射表随即失效可删。副产品：g1-g4 因 fixture 提供了真反例，`needs_review` 全部清空。

**这证明了约定 1 是有效的**——统一采集格式，就能换回归纳器的通用性。

### 剩余一处（性质已查明，不是架构问题）
`main()` 里 `if flow_name == 'changeset-audit'` 分派到独立管线 `build_graph_changeset_audit`。

主会话实测（<日期>）：把该流程轨迹改名后交给**通用引擎**，报错退出——
> 未能为判决点 ['p1'..'p5'] 找到对应的决策步骤(既没有专属决策节点，也无法用结构性补齐规则从孤儿工具步骤配齐)

所以准确的说法是：**通用引擎目前服务 2 条流程，最早那条 `changeset-audit` 走的是专用管线，从未被通用引擎归纳过**。它的"字节级等价"回归证明的是"老管线没被改坏"，不是"通用引擎能吃它"。

根因**与已还清的那笔完全同源**：`changeset-audit` 是最早采的，那时约定 1 还不存在，它的 digest 里没有 `point=`/`verdict=` 字段。

**还清路径**：按约定 1 重采 `changeset-audit` 轨迹 → 通用引擎应能自动定位 p1-p5 → 删除专用管线与该分派。验收：通用引擎产出的图与现有 v1 语义等价（15 节点 / tool8 branch5 map1 llm1 / UNRESOLVED 0），且另两条流程回归不回退。

**在此之前，"归纳器完全通用"这句话要打折说：它对遵守约定 1 的流程通用（已验证 2 条），对约定之前采的老流程仍需专用管线。**

### 顺带发现的改进方向：让"跑图"就是"采轨迹"
运行器每跑一次图都会往 `trace_runs` 写节点级留痕，字段（节点、状态、输出摘要）与轨迹格式几乎重合，只差 `point=`/`verdict=` 约定。若把运行器的留痕直接按约定 1 输出，则：
- 图上线后每次运行都在**自动产生新轨迹**
- 新轨迹可重新归纳，用于发现判据漂移、补充稀缺反例、改进图
- `changeset-audit` 的重采也不必重新执行业务动作，直接从既有运行记录导出即可

这会把"编译一次"变成"持续自我改进"，是这套东西的完整形态。

## 硬约定（续）

6. **归纳出的图必须真跑过才算数**
   归纳器会把轨迹 digest 里的**摘要写法当成命令原文**，产出"看着对、实际不可执行"的伪代码。
   - 事故（<日期>）：图① 的行尾扫描节点被归纳成 `for f in <non-delete files>; do git show {sha}:"$f"` —— `<non-delete files>` 是采集位写在摘要里的占位符，不是 shell 变量，`git show` 100% 报路径不存在。**这条判据从归纳出来到被病态仓库验证之前，一直是废的**，而它恰恰是抓到 <项目>-theme 13 个 CRLF 文件的那一项（当时是采集脚本抓的，不是图抓的）。
   - 规矩：任何图在接线前必须在**自建病态 fixture** 上跑出全部 FAIL 分支，并用一个干净 fixture 反向验证不会恒 FAIL。零反例的判决点不许上线。
