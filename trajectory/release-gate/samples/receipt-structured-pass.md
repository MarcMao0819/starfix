# CS-DEMO-STRUCTPASS — 样例回执（A1 结构化判词行·PASS 场景）

> 本文件是 release-gate 的测试样例，不是真实回执，仅用于验证 A1 三级判据的第一级
> （结构化判词行）在 `verified_by=校验官 / verdict=PASS / anchor=<与登记一致>` 场景下
> 判 PASS。刻意放在 `release-gate/samples/` 下，不进真实回执目录
> `${FLEET_HOME}/<项目>ERP/迁移备份/回执/`。

- **状态**：待校验官验收（承建方自报占位文本，验证时不应影响判决——一旦下方结构化
  判词行存在，A1 应直接采信该行，不应退回到这句自报文本的启发式判断）

## 校验官判词（结构化，机器可读）

verified_by=校验官 / verdict=PASS / anchor=6f8d0325f8f994ce77f08d24a6ec42b13dc0c118 / ts=<日期>T12:00:00Z
