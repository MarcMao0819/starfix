# 判例库（真实生产判例，供判据设计与争议参照）

## 行尾类

### 【正反成对】0018 违规 → 0019 还原（<日期>，首个"还原修复不误判违规"生产判例）
- **0018（违规）**：行尾被翻，机检判 FAIL —— 该拦的拦住了
- **0019（还原）**：阿洁的行尾回滚提交，机检走 **p5 → NEEDS_HUMAN**，**没敢机判**；主窗口按还原三判据（算术闭合 339 / `diff -w` 为空 / 自证链条）人工判 **PASS**
- **价值**：这对样本证明了判据在**两个方向**都成立——**能拦破坏，也不误伤修复**。单看任一边都不足以说明判据对。
- **关联**：还原三判据的②③分辨不出方向（CRLF→LF 与 LF→CRLF 同样满足内容零变），方向只能靠①算术闭合，而①需要 `restore_target` 声明。见 SPEC 相关节。

### 【误报，已修】<项目>-theme 13 文件 / CS-<日期>-0055（<日期>）
- 旧判据「改后 CR>0 即 FAIL」把**存量就是 CRLF 的文件的正常改动**判成违规，两笔告警全是误报，其中一笔导致签3 白清洗一趟
- 修法：改判增量（`改后CR = 改前CR + 新增CRLF - 删除CRLF`），并加 `--ignore-cr-at-eol`

## 冲突标记类

### 【误报＋空转，已修】g3 判据只有三分之一在工作（<日期>，fixture 实证，非生产事故）
- **空转**：旧正则 `^(<<<<<<<|>>>>>>>|=======)$` 要求整行**恰好等于**那七个字符。可 git 写出的开头/结尾标记必带标签（`<<<<<<< HEAD`、`>>>>>>> other/side`），带后缀就匹配不上。把真冲突块提交进 fixture 逐分支实测：`^<<<<<<<$` 命中 **0**、`^>>>>>>>$` 命中 **0**、`^=======$` 命中 **1**——**三个分支只有中间那行光秃秃的等号真正生效**，另两个从上线起就是死的。
- **误报**：markdown 的 setext 一级标题下划线正是一串 `=`，恰好七个时与唯一生效的那个分支完全重合。一份正常文档实测被判 FAIL，整个候选会被拦下。
- **为什么一直没被发现**：真冲突照样能抓到（靠等号那行），**结果看起来是对的**。只有把三个分支拆开单独跑，才看得出检出力其实只剩三分之一。
- **修法**：三种标记一律允许带标签后缀（使其真正生效），同时移除光秃秃的 `=======`——真冲突必定同时带开头与结尾标记，去掉中间那行不损失检出力，却消掉了与正常文档唯一的撞车面。
- **已知边界**：正文合法展示冲突块示例的教学文档仍会命中，属**显性**误报（人一眼能判），与 setext 标题那种毫无线索的误报不同，接受。
- **两版对照实证**：真冲突块 v6/v7 都 FAIL；cherry-pick 冲突态 v6/v7 都 FAIL；markdown 文档 v6 **FAIL**→v7 **PASS**；干净样本两版都 PASS。
- **方法论**：这是**总纲的第三次复现**——只验了"真冲突能抓到"，没验"抓到它靠的是哪一条、正常文档会不会被抓"。同时也是**多分支判据的通病**：只要有一个分支能兜住结果，其余分支是死是活从整体判决上完全看不出来。**判据含 `|` 分支的，必须逐分支单独取证。**

## 登记类

### 【拆分记账伪影】CS-<日期>-0052 / 0053 同锚多 changeset 拆分登记（<日期>）
- **现象**：两单同锚点 `640ea6810`、同分支 `feat/printtpl-xiaolu`，各登记 7 / 5 文件（`delivery/index.vue` 共有），机检 p2/p3 双 FAIL
- **实测**：并集去重 **11** 文件，与 `0575074b5..640ea6810` 的实际 diff **11** 文件**逐字相等** → **记账闭合，非真缺陷**
- **根因**：单张单的文件集 ⊊ 全 diff。判据按"单张登记 vs 全 diff"比对，拆分登记必然报错
- **★ 与 CS-<日期>-0052 的区别（务必分清，两者都叫 p2/p3 FAIL）**：
  | | 0807-0052 | 0808-0052/0053 |
  |---|---|---|
  | 类型 | **基线找错** | **拆分记账伪影** |
  | 现象 | 登记 115 / `^1..锚点` 只有 1 | 登记 7 与 5 / 全 diff 11 |
  | 根因 | n9b 沿第一父链搜不到含 merge commit 分支的 merge-base | 一个锚点被拆成多张单登记，单张 ⊊ 全量 |
  | 修法 | 基线三级解析（已修，v5.1） | **未修，见下方意见** |
  | 判别法 | 换基线口径后能对上 | 换基线也对不上，**要把兄弟单并起来才对得上** |

- **主窗口裁定（<日期>）：图不改，纪律根治，方案挂起至复发。**
  - **①线上图维持现行**（拆分登记判 FAIL → 人工兜）：今天首例，机制照设计工作了（告警 → 主窗口核并集 → 分钟级闭合），成本可接受
  - **②根治在派工侧，不在判据侧**：任务书今后写死「**一任务一 worktree 一分支一锚，严禁在他人任务 worktree 内落第二张单**」。本例是**纪律偏差不是图缺陷**（守卫单被落进了打印单的 worktree）
  - **③声明方案（`split_group` + `SPLIT_REGISTRATION` 三段判定）存档为附案，该形态第二次出现即启用**；启用顺序：先与登记侧约定字段，再改图
- **反对自动聚合的三条理由（已采纳入正文，供复发时直接引用）**：
  1. **打破判决独立性**——按同锚聚合后，一张单的判决依赖别的单是否存在；任何人在同锚再登一张就能改变前一张的判决
  2. **时序不可控**——先登记的那张必判 FAIL，兄弟登记后复扫又都过，**代码一个字没变、判决却变了**；机器无法预知未来的兄弟单，抖动靠聚合逻辑消不掉
  3. **误聚合**——"同分支同锚"不是充分条件，两个不相关交付碰巧同锚会被错并成一组
- **附案的要害（复发启用时务必保留）**：**并集 == diff 只证明"合起来对得上"，不证明"每张单的划分是对的"**。故 `SPLIT_REGISTRATION` 语义为「合计闭合，**划分待人工确认**」，**不等于 PASS**



### 0007 / 0017（<日期>，分诊不放过登记烂单）
- `branch` 字段为模板残留错值、明细含乱码与仓库外文件 → **重扫仍判 FAIL**
- **意义**：验证了入口分诊的边界划得对——**分诊只放过"已并线且审过"的，不放过"登记本身就错"的**。若这类单被分诊跳过，就是把"消噪音"做成了"消证据"

### CS-<日期>-0052（<日期>，多提交必误报，已修）
- 登记 115 文件，`^1..锚点` 仅 1 文件 → p2/p3 纯误报
- 根因：n9b 沿第一父链搜不到含 merge commit 分支的 merge-base
- 修法：基线三级解析（`^1` → `merge-base` → `~N`），逐候选验文件集合逐字相等，加 `base==head` 断言
