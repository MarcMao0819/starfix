# 监视清单 · 「纯后端含迁移」候选单（影子期最后一类缺失形态）

**用途**：影子期形态覆盖里只差这一类。本清单定义"什么样的 changeset 一进来就能当候选单"，主窗口照单认领即可，不必每次现想。

## 认领判据（全部满足才算合格候选）

| # | 判据 | 怎么查 |
|---|---|---|
| 1 | **含数据库迁移** | 改动文件含 `db/migrations/*.sql`，或任何 DDL（`ALTER`/`CREATE TABLE`） |
| 2 | **纯后端** | 改动文件**不含** `frontend/`；主体为 `src/main/java/**` |
| 3 | **未并线** | `git merge-base --is-ancestor <锚点> feat/<项目>-integration` **不成立** |
| 4 | **判词已落** | 回执含结构化行 `verified_by=校验官 / verdict=PASS / anchor=<40位> / ts=` |
| 5 | **登记完整** | `file_count` == 明细行数 == `基线..锚点` 的 diff 文件数 |
| 6 | **一任务一锚** | 该锚点下**没有兄弟 changeset**（避开拆分登记形态，见 CASES.md） |

## 这一单验什么（**与其它形态不同，请勿套用常规预期**）

- **准入必然判 `NEEDS_HUMAN`**（A5 命中迁移触发）——**这就是要验的东西**：验准入**能否正确识别出"这单要人工兜"**，而**不验执行**。
- 所以本单的合格结果是「**正确地不放行**」，不是「顺利跑完 B0-B5」。若它被判成 PASS 并渲染出执行单，**那才是缺陷**。
- **回滚路径不验**：蓝图里已定「迁移已执行 + 重启失败 → 不自动回滚、直接转人工」，那条是执行器阶段的事，影子期不碰。

## 影子期计数口径

- 该单**计入 8 单序列**（它验的是准入分诊的正确性，属管道能力）
- 分歧判定照常按三级分级
- 执行环节由主窗口人工兜（迁移先于重启需人判），**人工兜的过程不计入管道分歧**，但过程中若发现单面给的信息不足以支撑人工决策，**那算管道分歧**（单子的职责是让人有据可依）

## 目前状态

**未出现合格候选**。近期 `data_migration` 类登记（如 CS-<日期>-0050）属**非代码交付**，会被入口分诊判 `N/A` 短路，**不满足判据 2**，不能当本形态候选。
