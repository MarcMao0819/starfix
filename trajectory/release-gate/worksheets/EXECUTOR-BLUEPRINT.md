# 发布执行器 · 蓝图（待 Owner 签字）

**终点形态**：单次一 commit 上线，把主窗口从部署里解放出来。
**本文只是蓝图，不含施工。** 执行器上岗的前提写在 §6，不满足不许上。

---

## 1. 它是什么、不是什么

**是**：一个把已经通过准入的操作单，按机器轨迹自动执行下去的程序。输入是一份准入 PASS 的 worksheet，输出是执行结果 + 一条合规轨迹。

**不是**：一个"自动发布系统"。它**不判断该不该发**——那是准入（A1-A5）和校验官的事；它只负责**把已经批准的事，一步不错地做完**。

这个区分是整套设计的地基：**判断权留给人和准入，执行权才交给机器。**

---

## 2. 执行边界：机器做什么，人守哪三道闸

| 步骤 | 谁做 | 说明 |
|---|---|---|
| B0 获取锁 + 记录并线前基点 | **机器** | 串行锁，见 §5 |
| B1 并线 `merge --no-ff` | **机器**，冲突即停 | 干净合并机器做；**一旦冲突立即停下交人判**，绝不自动解冲突 |
| B2 前端类型检查 | **机器** | 非零退出即停 |
| B3 后端打包 | **机器** | 打包前自动备份旧 jar（回滚素材） |
| **B4 重启** | **机器执行，但执行前必须拿到主窗口放行** | **人闸一** |
| B5 探针与断言 | **机器** | 含双条件断言、否定式配肯定式 |
| 收尾（释放锁 / 回执 / 台账行） | **机器** | 轨迹自动落盘 |

### 三处人闸（不可绕过）

1. **B4 重启前，必须主窗口放行**。理由：重启是这条链上唯一"影响所有在线用户"的动作，且失败形态最重（旧进程假绿、新进程撞端口、jar 已换构成随机 5098）。放行请求要带上前面各步的实测证据，让放行的人有据可依，而不是盲签。
2. **任何 FAIL / AMBIGUOUS 立即停下交人判**。执行器**不许自行重试掩盖**，不许"再跑一次看看"。停下时要保留现场（不自动回滚），由人决定是回滚还是就地修。
3. **准入 NEEDS_HUMAN 照旧转人工**，执行器根本不接这类单（含迁移候选、非代码交付、已并线单）。

---

## 3. 回滚怎么机械化

**回滚点定义**：B0 记录的"并线前基点"（集成线当时的 HEAD）+ B3 前备份的旧 jar 路径。这两样在 B0/B3 就必须落盘到锁文件里，**不是出事了再去找**。

| 失败步骤 | 机械回滚动作 | 是否需人 |
|---|---|---|
| B1 冲突 | `git merge --abort` | 机器可自动，然后停下报人 |
| B2 tsc 失败 | `git reset --hard <并线前基点>` | 机器可自动，然后停下报人 |
| B3 打包失败 | 同上（未产生新 jar） | 机器可自动 |
| B4 重启失败（**迁移未执行**） | kill 新进程 → 用备份旧 jar 重启 → 验证恢复 → 再 `reset --hard` 基点 | 机器执行，完成后停下报人 |
| B4 重启失败（**迁移已执行**） | **不自动回滚，直接停下转人工** | **必须人**：迁移多为不可逆的加列/建表，机械回滚会造成二次破坏 |
| B5 探针失败 | 切回备份 jar，保留现场 | 机器执行，停下报人 |

**硬规矩**：回滚动作执行完必须**独立回读验证**（新 PID、jar mtime、探活、`is-ancestor` 复核），不许拿"回滚命令返回 0"当恢复成功——这是防御性发布三则的第一条。

---

## 4. 与影子期计数的关系

**执行器上岗 = 影子期毕业。** 这不是先后关系，是同一件事的两面：

- 影子期在验的是「**管道出的单，人照着执行不会遇到问题**」
- 执行器要做的是「**把人照着执行的那套，交给机器做**」
- **单子本身有毛病，交给机器只会错得更快**

所以顺序不可颠倒：**先 8/8 零分歧，再谈执行器**。当前 **3/8**。

**毕业条件（缺一不可）**：
1. 连续 **8 单零分歧**（按三级分歧分级口径：静默级重置计数／显性阻断级不计但不重置／轻微级仅记录）
2. 形态覆盖齐：纯前端／纯后端／前后端混合含重启／含迁移走 NEEDS_HUMAN／已并线分流 —— **目前缺"纯后端含迁移"一类**
3. **Owner 签字**

---

## 5. 锁与并发：怎么防两个执行器互咬

**现状的锁是人工纪律**（生成器只读锁状态、不写不删），执行器上岗后必须变成**强制锁**。

| 机制 | 设计 |
|---|---|
| 锁文件 | `release-gate/.locks/CURRENT.lock`，内容含 `changeset_no / operator / acquired_at / 并线前基点 / 旧jar备份路径` |
| 获取方式 | **原子创建**（`O_CREAT\|O_EXCL`，不是"先查再建"——那有竞态窗口） |
| 已有锁时 | **直接拒绝启动**，打印持有者信息，不排队不等待（发布不该排队，该让人看一眼为什么有人在发） |
| TTL | 设过期时间；**过期不等于可抢**，要走"僵死锁确认"流程：核对持有者进程是否还活着，人确认后才能清 |
| 释放 | 收尾步骤释放，且**释放后回读确认锁文件确实没了** |
| 崩溃残留 | 执行器异常退出时锁会留下——**这是有意的**：留着锁比自动清掉安全，人来看现场比机器自作主张好 |

**并发红线**：同一时刻只允许一个执行器实例。锁的判据不看进程数，看**锁文件**——因为执行器可能跨会话、跨机器启动。

---

## 6. 上岗前置（硬闸，不满足不许上）

1. 影子期 **8/8 零分歧** + 形态覆盖齐 + **Owner 签字**
2. **B4 人闸的放行通道要先跑通**（放行请求怎么发给主窗口、怎么确认收到、超时没回怎么办——超时必须是"停下等"，不是"默认放行"）
3. 强制锁（§5）实现并验证过并发拒绝
4. 回滚路径（§3）**每一条都在病态 fixture 上真跑过**——按第 6 条纪律，零反例的分支不许上线
5. 执行器自身要有**只读守卫的反面**：它必须能写（merge/package/重启），所以要有**白名单式的"允许写"清单**，清单外的写操作一律拒绝执行

---

## 7. 已知风险（写在前面，不等出事再补）

- **执行器会写生产**。这是整条线上第一个有写权限的部件，此前四张图与操作单生成器全部是只读的。风险等级跃升一档。
- **模型兜底的不确定性会进入执行链**：AMBIGUOUS 走模型，模型有约 5-10% 不稳定（超时/解析失败）。执行器遇到这种必须**停下交人**，不许"重试到通过"。
- **单子质量决定执行质量**。影子期至今抓到的管道缺陷（kill 命令缺过滤、日志相对路径、探针占位符、判据字面比较）**每一个若发生在执行器身上，都是自动化的错误**——这就是为什么毕业条件不能放水。

---

## 8. 一句话

**先把单子做对，再把执行交给机器。** 现在 3/8，离上岗还有 5 单零分歧、一类形态、一份签字。
