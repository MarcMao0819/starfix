# 轨迹编译 · 交接给 <跳板机> 项目（erp-project）

写给要在 <跳板机> 上从零建这套东西的 agent。本机（<项目>线）已跑通四条流程、三张图投产，本文是把经验搬过去的完整规程。**先读完再动手**——里面有三处"看着成了实际废了"的血案，不知道会重蹈。

---


## 〇、先读这一条：所有坑的公因式

两天里所有事故（误报、漏报、假绿、验证盲区）套进同一个模子：**只断言了"该发生的发生了"，没断言"不该发生的没发生"。**

| 只验了 | 漏验了 | 后果 |
|---|---|---|
| 文件里有没有 CRLF | 这是本次引入的还是本来就在 | 存量文件的正常改动被误报 |
| grep 找到了什么 | 找不到是不是工具差异造成的 | 假阴性，差点让人返工 |
| diff 有没有内容 | base 是不是等于 head | 空 diff 静默通过，**迁移漏报** |
| 名字像不像临时资源 | 它是不是正在被使用 | 在用的资源被当垃圾报 |
| 探活成功了 | 答复的是新进程还是老进程 | 老进程假绿，构成运行时雷 |
| fixture 造了"本次引入问题" | 没造"存量带病、本次无辜" | 判据的误伤面从未被验证 |
| 弹窗消失了 | 该保留的横幅还在不在 | "全关掉"被当成"修好了" |

**每写一条判据、每造一批 fixture、每设一个断言，都要问第二句：反过来会怎样？不该报的会不会被报？该报的会不会静默地不报？**

下面十条铁律，都是这条总纲在具体场景里的落地。

## 一、这是什么、解决什么

**现状**：agent 每接一个重复任务都要现场"想一遍"——同样的活，token 每次重新烧，结果还偶尔跑偏。

**做法**：把干活的过程录成轨迹（每一步执行了什么命令、拿到什么输出、最后判了什么），从多条轨迹里归纳出一张**确定性执行图**，之后同类任务直接按图跑：稳定步骤由程序执行（零 token），只有遇到没见过的情况才叫模型来判。

**本机实测**：变更单核对流程，原本每单约 1.2 万 token / 分钟级，编译成图后**常规单 0 次模型调用 / 0.6 秒**，48 轮真实运行零静默错误。并线前门禁图已成为主窗口每次并线的机检前置（1.2 秒 / 0 调用）。

**不要误解的地方**：值钱的是"确定性图 + 岔路口才调模型"，不是把图编译成二进制（图本身的解释开销近似于零，编译成二进制省不了钱）。

---

## 二、三个部件

| 部件 | 职责 | 能不能直接搬到 <跳板机> |
|---|---|---|
| **归纳器** `inducer/induce.py` | 吃一个目录的轨迹 jsonl，吐出工作流图 JSON | **能直接搬**（纯 Python 标准库，不碰环境） |
| **工作流图** `graphs/*.json` | 人可读可审的判据编排：确定性节点 / 分支 / 循环 / 模型节点 | **不能搬**（判据是各自流程的），但格式与字段照抄 |
| **运行器** `runner/run_graph.py` | 照图执行，带只读守卫、SQLite 留痕、偏离检测、模型兜底 | **需适配**：命令执行层要换成 <跳板机> 的方式（见 §五） |

图的节点四类：`tool`（确定性执行）、`branch`（谓词分支）、`map`（对清单逐项执行）、`llm`（岔路口调模型）。顶层还有 `classification`（文档/对象分型门控）、`fallback`（全图兜底出口）、`conventions`（约定条款）、`revision_log`（改版留痕）。

---

## 三、七步流程（照做，别跳步）

**Step 0 前置硬闸** —— 不过闸就别做
1. 目标流程要有 **≥10 条可解析的历史轨迹**（或能真跑出来）
2. 对样本做分析，量化"确定性步骤 vs 需模型判断步骤"的比例，**预估省耗 <30% 就别建图**，承认那条流程就该模型跑

**Step 1 采样** —— 必须覆盖所有已知分支，**必须包含失败样本**。只挑成功样本会编出只认识晴天的图。

**Step 2 归纳** —— 时间风险集中点，允许单独超时。稳定判据：≥80% 轮次中工具名与参数骨架一致 → 确定性节点；归纳不出谓词的分支 → 标成模型决策节点，不要硬编机械规则。

**Step 3 人工审图** —— **不可跳过**。本机三次审图，每次都抓到致命问题（见 §四）。

**Step 4 病态 fixture 验证** —— 自建病态样本，把**每一个报警分支都真触发一遍**，再用一个干净样本反向验证它不会逢查必报警。

**Step 5 历史案例对跑** —— 拿已有人工结论当标准答案逐条核对，不符的先查是图错、运行器错、还是现场状态变了。

**Step 6 交接接线** —— 报告里要写清残留风险与未验证分支。

---

## 四、十条铁律（每条都是血换的，附血案）

### 1. 判决步骤的记录必须带判决字段
轨迹里每个决定判决点的步骤，输出摘要必须含 `point=<判决点>` 与 `verdict=<PASS|FAIL|N/A>`。

> **血案**：本机三条流程各写各的格式，其中一条只记统计值（`tracked_dirty=0 untracked=3`）不记判决，归纳器无法把判决点关联到步骤，被迫在代码里写了一张**人工映射表**——通用性当场打折。后来按本条重采，映射表立即失效可删。

### 2. 判据实现必须在受版本控制的仓库里
判据脚本放 `runner/checks/`，**禁止留在会话临时目录**。

> **血案**：某张图的每个节点都指向临时目录里的 `check.py`，临时目录一清、换台机器，整张图就是个空壳。审图时才发现，抢救入库。

### 3. 禁用裸 `grep`
存在性检查一律用 `/usr/bin/grep` 绝对路径或 Python 正则；禁用 `\w`/`\d`/`\s` 这类在不同实现间行为不一的简写。**<跳板机> 上第一件事就是验证 `type grep` 是不是函数包装。**

> **血案**：本机 `grep` 是 ugrep 函数包装，与子进程里的系统 grep 行为不一致。同一天两起误报：一起把 9 份合格文档判成不合格；一起把"某文件缺 40 位提交号"报给了调度方，对方**已准备派人返工**，紧急撤回才没造成白工。存在性检查天生"没找到=判违规"，工具差异造成的假阴性会直接变成对人的错误指控。

### 4. 同构优先于优雅
每轮必须跑**完全相同**的步骤序列，不适用的检查项也要跑并记 `N/A`。禁止为省事合并或跳过步骤——同构是归纳的前提。

### 5. 必须带反例，且两态齐全
- 某判决点若在全部样本里从未 FAIL，它的报警分支就是**未经验证的人工规则**，必须标记并在上线前用病态 fixture 证明可触发。
- 反过来同样致命：某判决点若从未 PASS（本机收尾体检就是全程 FAIL），图可能学成**逢查必报警**，跟坏掉的烟雾报警器一样。做法是把"扫描范围"参数化，用自建资源范围采出通过态。

### 6. 归纳出的图必须真跑过才算数
> **血案（最狠的一条）**：归纳器会把轨迹摘要里的**占位符当成命令原文**。本机门禁图的行尾扫描被归纳成 `for f in <non-delete files>; do git show ...`——`<non-delete files>` 是采集时写在摘要里的占位符，不是 shell 变量，`git show` 100% 报路径不存在。**这条判据从归纳出来到被病态仓库验证之前一直是废的**，而它恰恰是抓到某分支 13 个 CRLF 文件的那一项（当时是采集脚本抓的，不是图抓的）。图看着一模一样，实际一个都抓不到，还会报"全部通过"。

---

### 7. 判据里的阈值必须参数化
硬编码阈值（滞留天数、运行时长、积压数量）会让带时间累积门槛的判决点**在一次会话内造不出反例**，两态永远凑不齐。采样时把门槛调成 0 取报警态、调回生产值取通过态，判据结构不变。附带好处：同一张图既能自检（小范围严阈值）也能巡检（全局宽阈值）。

### 8. 不同判据版本的样本不许混用
判据一改，此前采的轨迹即作废、移入存档目录并注明。
> **血案**：某流程字面上"两态齐全"，实为**通过态出自新判据、报警态出自旧判据**——混着归纳会把已被否定的旧规则学回图里。

### 9. fixture 必须覆盖"存量带病、本次改动无辜"
只造"本次引入问题"的病态样本不够，还要造"问题早就在那、这次没碰它"的样本。
> **血案**：行尾判据只验了"新增一个整篇 CRLF 文件"，没验"存量就是 CRLF 的文件正常改几行"。结果两笔"并线阻断级"告警全是误报——文件改前改后 CR 完全相等、增删行数相等，存量行尾一根没动。误报被采信去执行清洗（整体转 LF），才是真正的破坏。
>
> **推论**：凡检查"状态"的判据（有没有 CRLF、有没有脏、有没有残留），都要问一句——**这是本次造成的，还是本来就在？判绝对状态会误伤，判增量才对。**

### 10. 改共用运行器时旧图必须保持可跑
运行器是多张图共用的，但节点实现与图的命令模板是**耦合的**。就地改实现，等于让所有还在用旧图的生产调用当场失配。
> **血案**：升级判据时就地改了运行器的解析格式，而生产轮询器仍在用旧图 → 解析出畸形结果、聚合节点直接崩、**投产的自动核验断了**。报修方一度以为是数据脏。
>
> **规矩**：① 节点实现按图版本或命令模板形状分派，旧图必须原样跑通且判决不变；② **在副本上改、原子替换**（`cp` 新文件 → 编辑 → 验证 → `mv` 覆盖），**禁止活体编辑正在被调用的脚本**；③ 动手前先通知在用方暂停，双向知会。

---

## 四之二、判据设计的三个静默陷阱（最该带走的一节）

今天所有事故收敛到同一个形状：**检查失效时是静默的，而且表面全绿**。三个来源：

| 陷阱 | 表现 | 例子 |
|---|---|---|
| **判绝对状态而非增量** | 把历史存量算到当前改动头上 | 「文件里有 CRLF 就违规」→ 存量 CRLF 文件的正常改动被误报 |
| **基准选错** | diff 为空 → 什么都查不到，却报"通过" | 共享对象库里 `merge-base(集成线, HEAD)` **会退化成 HEAD 自己**，导致清单核对误报、行尾检查 `checked=0` 假绿、**迁移文件漏报被当普通候选放行** |
| **工具差异** | 同一条命令在不同环境给出不同答案 | 交互 shell 的 `grep` 是函数包装，与子进程的系统 grep 行为不一致 → 存在性检查假阴性 |

**共同的防御动作**：
1. 任何"没找到 = 判违规"的检查，结论落地前用**第二条独立路径复验**（Python 与命令行互证）
2. 求"这次改动了什么"的基线，**加断言禁止 base == head**，绝不允许静默产出空 diff
3. 基线解析出来后要**喂给所有依赖它的检查项**，漏喂哪一项哪一项就静默失效
4. 每个判据问一句：**这是本次造成的，还是本来就在？**

## 五、<跳板机> 特有的适配点（红线优先）

### 红线（违反即事故）
- **<跳板机> 是生产系统**。首批一律只做**只读检查类**，不碰任何写操作。
- 判据里的数据库访问走只读通道（`run_sql`），**禁 `execute_sql`**；命令执行不得触及部署、重启、发布管道。
- **病态 fixture 绝不在生产上造病**。要造脏树/冲突/残留，一律在自建的临时目录或临时库里造，用完清零并贴证据。
- 运行器的只读守卫（命令前缀白名单 + SQL 载荷必须以 SELECT 开头）**照搬，且要按 <跳板机> 的工具集重新配白名单**。

### 环境差异清单（搬之前逐条确认）
| 项 | 本机（<项目>线） | <跳板机> 需确认 |
|---|---|---|
| 命令执行 | 本地 shell | 走 MCP 工具还是 ssh？运行器的执行层按此改写 |
| 数据库 | docker 容器内 mysql | `erp` 库，只读通道 |
| git | 本地仓库 + worktree | `${PROD_REPO}`，多会话共享 |
| grep | **是 ugrep 函数包装（有坑）** | **必验**：`type grep` |
| 模型兜底 | `claude -p --model sonnet` | 确认 CLI 可用性与超时行为 |
| 留痕 | 本地 SQLite | 建议同样落本地 SQLite，别写生产库 |

### git 只读铁律
所有 git 命令带 `--no-optional-locks`（防刷新别人的 index）。共享仓库上有别的会话在实时干活，任何写操作都是事故。

---

## 六、<跳板机> 上的候选流程（按建议顺序）

判据四条：**高重复、步骤同构、判据大部分机械可判、能拿到反例**。

1. **变更单登记核验**（首选）：`t_code_changeset` 与 git 提交的互洽——提交号是否 40 位全 sha、明细行数与实际改动文件数是否一致、清单是否逐字对齐、标题有无乱码、行尾是否规范。本机第一张图就是这个，**判据和图结构可以直接参考**，只需换数据坐标。
   - 已知坑：多提交特性分支的基线不是 `锚点^1`，要沿第一父链搜到与明细逐字相等的基线；中文文件名要加 `-c core.quotepath=false` 否则假报警。
2. **发布前门禁**：Release 管道本身已是人肉编译的状态机，把前置检查编成图。注意只做检查，不碰发布动作。
3. **模块注册点核验**：新模块的权限码在多处是否逐字一致——纯 grep 型，样本现成。
4. **运行时错误巡检**：定时扫 `t_runtime_error_log` / `t_request_anomaly_log` 的未处理项并分类。
5. **收尾体检**：临时库/临时账号/僵尸授权/未停实例/分支积压。
   - **本机踩过的判据坑**：不能拿"名字像临时资源"当"是垃圾"——正在用的资源名字也一样。正确判据是"**没人在用且超期**"：库要加"无活跃连接 + 滞留超 N 天"，实例要加"运行时长超 M 小时"，且**常驻服务端口必须硬编码豁免**（否则每次都报警，两周后没人再看告警）。残留要"库+账号+授权"三件一体报，只报库会导致清一半留一半。
   - 另一个必现坑：`mysql.db` 的 `Db` 列存的是 LIKE 模式，**字面下划线被 MySQL 转义**、客户端输出时反斜杠再转义一次。不还原会把**几乎所有正常库名**（下划线命名的都中招）误判成"目标库不存在"的僵尸授权——不修则该项大面积误报，且报出来看着言之凿凿。

6. **发布/机务管道**（最重，含写操作，务必最后做）：并线→构建→重启→冒烟。
   - **素材形态陷阱**：历史台账多是**叙述性**记录（"新 PID 48117，启动 7.5s，ERROR=0"），不是可解析轨迹。硬从叙述文档挤轨迹会得到"看着对、跑起来废"的图。正确顺序是**先影子跑攒真轨迹，再编译成图**。
   - **上线方式必须是影子模式**：管道只出判决和操作单，人照单执行、回填实际结果，连续 N 单零分歧才谈自动（本机 N=8，覆盖四种交付形态）。影子期**管道零写操作**，脚本里用白名单守卫封死。
   - 把回填表直接设计成**键值串形态、字段名预填只留 `?`**，这样人填完的操作单本身就是一条合规轨迹，编译时零转换。
   - 准入不要靠猜文本：让上游在回执里写**结构化判词行**（`verified_by=` / `verdict=` / `anchor=` / `ts=`），机器直接读；关键词启发式只作兜底并标低置信。`anchor` 还能当第二道防线——**判词锚点与登记锚点对不上就拦**，治"验收后又追加提交"。

**不适合的**：写业务数据的操作（错了要赔钱，等只读层稳定几个月再谈）；创作类（写代码、做设计，每次都不一样，没有轨道可言）。

---

## 七、验收标准（硬线，不达标即止损）

1. 同批任务（含**从未参与归纳的新样本** + 人为扰动数据）上，业务正确率不低于原来的做法
2. 模型调用 / token 消耗下降 **≥30%**
3. 偏离已知路径时 **100% 触发兜底**，不允许静默走错
4. 每个判决点的**报警分支都在病态 fixture 上真触发过**，且有干净 fixture 反证不会恒报警
5. 未覆盖分支率、兜底次数有报表可查；滚动兜底率超阈值触发重新归纳
6. **不达标就停，结论与数据记档——方向证伪也算合格产出**

---

## 八、可直接复用的资产（本机路径）

```
${TRAJ_HOME}/
├── inducer/induce.py              # 归纳器（可直接搬）
├── runner/run_graph.py            # 运行器（执行层需适配 <跳板机>）
├── runner/checks/receipt_checks.py# 判据脚本样板（入库、纯 Python、不用 grep）
├── graphs/changeset-audit.v4.json # 变更单核对图（<跳板机> 首选流程可参照）
├── graphs/premerge-gate.v5.json   # 门禁图（含 map 循环节点样板）
├── graphs/receipt-compliance.v2.json # 合规图（含分型门控与模型节点样板）
├── graphs/KNOWN-LIMITS.md         # 各图原理上查不出的边界（这类东西必须显式记录）
└── TRAJECTORY-CONTRACT.md         # 六条约定全文 + 技术债台账
```

轨迹样本在 `${TRAJ_DATA_DIR}/`（五组共 72 轮），可作格式参照。

---

## 九、开工第一步

不要一上来就写代码。按顺序做三件事：

1. **验环境**：跑 `type grep`（确认有没有包装坑）、确认命令执行通道、确认模型 CLI 可用
2. **选流程**：从 §六 挑一条，做 Step 0 的两道闸（轨迹够不够、省耗预估过不过 30%）
3. **采 10 轮**：严格同构，判决步骤带 `point=`/`verdict=` 字段（约定 1），含失败样本

三件事做完再谈归纳。**Step 0 不过闸就换流程或止损，这比做出来一个没人用的东西便宜得多。**

---

## 十、一句话总结

这套东西的价值不在"AI 更快"，在**干过的活自动沉淀成资产，越干越便宜**；而它最大的风险是**假成功**——图看着对、跑着绿，实际什么都没检查。六条铁律全都是为了防这一件事。
