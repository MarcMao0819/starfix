# <单号> 回执（关键词 `<KEYWORD>`）

**语言 / Language：** [中文](02-回执.md) · [English](02-回执.en.md)

## ★ 终态：PASS / FAIL / BLOCKED

> 第一屏就要让人知道结论和最要紧的那件事。三态是硬的：
> **不许写"基本完成""应该没问题""大概可以"**。做不了就如实写 BLOCKED，
> 宁可交白卷——推测的结论比空白危险得多。

`<两三句话说清：做了什么、最要紧的发现是什么。如果过程中改过方向，在这里说，别藏在正文。>`

| 项 | 结果 |
|---|---|
| 工位 / 分支 | `<路径>` / `<分支名>` |
| 基线 | `<起点 commit>` |
| 终锚 | `<终点 commit>` |
| 登记 | `<变更登记号>` |
| 单元/契约 | `<N / 0F / 0E>` |
| 门禁 | `<结果>` |
| 红线 | `<逐条自证：哪些没碰>` |

---

## 一、改了什么

`<按文件/按判据讲清楚。贴关键代码片段，不要只说"改好了"。>`

## 二、★ 判据的判别力（每条都要答得出"它什么情况下会红"）

| # | 变异（把哪半条判据拿掉） | 该红的红了 | 只有它红 | 证据 |
|---|---|:---:|:---:|---|
| ① | `<变异描述>` | | | `<证据文件>` |

- 反例跑完必须**逐字节还原并复跑**，证明红是变异引起的。
- 若多红了别的条目，**如实记**并解释为什么合理——掩盖比多红更糟。

## 三、实测证据（L2/L3/L4）

`<贴原始输出，不是"我跑了，通过了"。>`

- 判据是「**被拒 + 拒绝理由对得上**」两条同时成立——只看"失败了"不行，
  失败可能是别的原因造成的。
- 状态码类断言必须配一个**实测为 200 的正例锚**；同码不同因要读响应正文区分。

## 四、数据回读

| 表 / 面 | 回读结果 |
|---|---|
| `<表名>` | `<字段级结果，含"应为空"的那些>` |

> 「调用发生过」不等于「数据留下了」：凡是"要留痕"的判据，单元测试之外
> 必须有一次真库回读——事务回滚会把留痕悄悄吃掉，而两种实现的日志一模一样。

## 五、越界发现（不在本单范围，只报不动）

1. `<看到但不该动的问题；写清为什么不动>`

## 六、残留风险

- `<你知道但没解决的；写清影响面和触发条件>`

## 七、收尾自证

| 项 | 证据 |
|---|---|
| 起过的服务 | `<探活 000>` |
| 一次性资源 | `<DROP + 清单回查为 0；不是只看计数>` |
| 别人的资源 | `<按名精确删的规矩，未碰>` |
| 临时凭据 | `<已销毁；全程未上命令行、未进回执>` |
| 未追踪残留 | `<git status --short 输出为空，或逐个交代>` |
