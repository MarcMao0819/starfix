# <topic> handoff (<outgoing party> → <incoming party>, <date>)

**语言 / Language:** [中文](03-交接.md) · [English](03-交接.en.md)

> The point of a handoff is not "I told you", it is that **the incoming party can finish the job on its own**.
> The criterion is simple: the incoming party works from this file and never has to come back with a second question.

## 1. Current state in one line

`<where it stands right now. Use state words, not adjectives: live / merged and awaiting deploy / under review / stuck at X.>`

## 2. Coordinates the incoming party needs

| Item | Value |
|---|---|
| Code location | `<path + branch + commit>` |
| Runtime location | `<host / port / database name, all written as placeholders or environment variable names>` |
| Where credentials come from | `<no values, only "where to get them">` |
| Related documents | `<absolute paths, ≤5>` |

## 3. ★ Preconditions (miss one and it will not run)

Write out **every implicit assumption**. The test: the incoming party follows this on its own machine and gets it running the first time.

- `<the arguments or environment variables required to start/run, listed one by one, each saying what happens if it is missing>`
- `<authorisations a human has to click to approve, permissions that must be requested in advance>`
- ★ Pay special attention to **configuration that has a default value**: leaving it out raises no error, it just quietly connects to the default target —
  for these you must write explicitly "must be passed; falling back to the default is not allowed".

## 4. Done / not done

**Done** (with the source of the evidence)
- `<what was finished + the receipt or evidence path>`

**Not done** (with the blocker)
- `<what is unfinished + where it is stuck + who to go to / what to do next>`

## 5. Pitfalls already hit (saves the incoming party time)

- `<symptom → root cause → fix>`. Write the root cause, not "watch out for this".

## 6. Pending decisions

| # | Question | Options | Who decides |
|---|---|---|---|
| 1 | `<question>` | `<A / B / C, each option stating its consequence>` | `<role>` |

> When you list options for the decision-maker, **list only options that can actually achieve the goal**. An action that cannot achieve the goal has no business being mixed in as an alternative.

## 7. Handoff confirmation

- [ ] The incoming party has run it through once in its own environment per §3
- [ ] The incoming party has confirmed who owns each pending decision in §6
- [ ] The outgoing party has stopped its own temporary resources (services / throwaway databases / temporary credentials)
