# <task ID> receipt (keyword `<KEYWORD>`)

**语言 / Language:** [中文](02-回执.md) · [English](02-回执.en.md)

## ★ Terminal state: PASS / FAIL / BLOCKED

> The first screen must already tell the reader the conclusion and the one thing that matters most. The three terminal states are hard:
> **never write "basically done", "should be fine", "probably works"**. If you could not do it, write BLOCKED honestly;
> better to hand in a blank sheet — a guessed conclusion is far more dangerous than a blank one.

`<two or three sentences: what you did, and what the most important finding is. If you changed direction along the way, say so here; do not bury it in the body.>`

| Item | Result |
|---|---|
| Worktree / branch | `<path>` / `<branch name>` |
| Baseline | `<starting commit>` |
| Final anchor | `<ending commit>` |
| Registration | `<change registration ID>` |
| Unit / contract | `<N / 0F / 0E>` |
| Gate | `<result>` |
| Red lines | `<self-proof line by line: which ones were not touched>` |

---

## 1. What changed

`<explain it file by file / criterion by criterion. Paste the key code snippets; do not just say "fixed it".>`

## 2. ★ Discriminability of the criteria (each one must answer "under what condition would it go red")

| # | Mutation (which half of the criterion was removed) | The one that should go red did | Only it went red | Evidence |
|---|---|:---:|:---:|---|
| ① | `<description of the mutation>` | | | `<evidence file>` |

- After running a counterexample you must **restore it byte for byte and rerun**, proving the red was caused by the mutation.
- If other items went red as well, **record it honestly** and explain why that is reasonable — covering it up is worse than the extra reds.

## 3. Measured evidence (L2/L3/L4)

`<paste the raw output, not "I ran it and it passed".>`

- The criterion is both halves at once: **rejected + the rejection reason matches** — "it failed" alone is not enough,
  because the failure may have been caused by something else.
- Any status-code assertion must come with a **positive anchor measured as 200**; when the same code has different causes, read the response body to tell them apart.

## 4. Data read-back

| Table / surface | Read-back result |
|---|---|
| `<table name>` | `<field-level result, including the ones that "should be empty">` |

> "the call happened" is not the same as "the data stayed": for every criterion about "leaving a trace", there must be one
> read-back against a real database on top of the unit test — a transaction rollback quietly eats the trace, and the logs of the two implementations look identical.

## 5. Out-of-scope findings (outside this task; report only, do not touch)

1. `<a problem you saw but must not touch; state why it is not touched>`

## 6. Residual risks

- `<what you know about but did not resolve; state the blast radius and the trigger condition>`

## 7. Closing self-proof

| Item | Evidence |
|---|---|
| Services started | `<liveness probe 000>` |
| Throwaway resources | `<DROP + list read-back is 0; not just a count>` |
| Other people's resources | `<the delete-by-exact-name rule; not touched>` |
| Temporary credentials | `<destroyed; never on the command line, never in the receipt>` |
| Untracked leftovers | `<git status --short outputs nothing, or account for each one>` |
